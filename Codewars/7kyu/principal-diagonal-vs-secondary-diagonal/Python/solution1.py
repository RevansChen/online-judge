# Python - 3.6.0

diagonal = lambda matrix: (lambda p = sum(matrix[i][i] for i in range(len(matrix))), s = sum(matrix[-(i + 1)][i] for i in range(len(matrix))): 'Draw!' if p == s else f"{'Principal' if p > s else 'Secondary'} Diagonal win!")()
